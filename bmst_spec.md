
# Be Music Script Tri-Digit (`.bmst`)
BMSに似た書式のテキストフォーマット

# 注: この文書は未完成です。

## 前提
- このフォーマットは`BMST`と略称する。
- この文書では、この種のファイルを解釈するプログラムを「解釈器」と呼ぶ。
- [_**MUST**_] と書かれた仕様には、解釈器は必ず従わなければならない。
  - [_**SHOULD**_] と書かれた仕様には、解釈器は可能なら従う。
  - [_**MAY**_] と書かれた仕様には、解釈器は従ってもよい。
- ゲームプレイに関わる仕様の一部(例:ノートを処理した際の得点処理)はこの文書では規定されない。この文書とは独立した規格が必要になる可能性もある。

## 概要
- BMSTはBMS(`.bms`)の限界を拡張する目的で策定される。BMSに対してBMSTは以下の点で異なる。
  - 記述方法が類似するコマンドはあるが、**BMSとの互換性は無い**。
  - 文字コードは`UTF-8`に限定される。Unicodeで定義された全ての文字種を(形式上は)利用可能である。
  - インデント可能。すなわち、各コマンド行は`#`の前に任意個数の空白文字を挿入できる。
  - 小節番号と定義番号の桁数が緩和される(後述)。
    - BMSにおいては36進2桁(合計`ZZ=1296`)または62進2桁(合計`zz=3844`)が上限であるが、BMSTでは3桁以上の定義番号が使用可能である。
    - 3桁(`ZZZ=46656`)でも62進2桁の約12倍の空間を有する。
    - 規定上、桁数の上限は無いが、実用上はフォーマット名にもなっている3桁で十分であろう。
  - シーケンスは単純な英数字の連続ではなく、`,`で各要素を区切る(後述)。
    - BMSで`11223344`と記述されていたシーケンスは、BMSTでは`11,22,33,44`となる。
      - ファイルサイズの増大と引き換えに、定義番号の区切りがより明白化される。
    - 区切り文字が必須化することで、定義番号を1桁で記述することも可能となる。(例: `1,2,3,4`)
    - 空白を表す`0`は省略できる。
      - 例: BMS:`11002233`→BMST`11,,22,33`
    - 連続する`0`は圧縮できる。
      - 例: BMS:`1100000000223300`→BMST:`11,*4,22,33,`
  - 条件分岐構文が、一般的なプログラミング言語の形式により近くなっている。
  - 一部仕様(例:ランダムなタイトル)は処理の煩雑さを軽減するため意図的に廃止される。
 - 以下は`BMST`形式で記述された譜面データの一部である。
```
#Title たのしいピクニック
#Artist ROKINA
#Bpm 150
// --以下ヘッダー省略--

#DefSound 1 bgm1.wav
#DefSound 2 bgm2.wav
// --以下定義省略--
#DefImage 1 title_320.png
// --以下定義省略--


```

# 詳細な規定
## ファイルフォーマット
- `.bmst`ファイルは**BOM無しUTF-8**形式で保存されたプレーンテキストである。
  - ファイルの内容は1行ごとに解釈する。[_**MUST**_]
  - 解釈器は改行コードが`CR` `LF` `CRLF`のいずれでも問題なく解釈しなければならない。[_**MUST**_]
- ファイルの内容は`#`で始まる「コマンド行」と、それ以外の文字で始まる「コメント行」からなる。
  - 行が1個以上の空白文字に続く`#`で始まる場合、解釈器はこの空白文字を無視してコマンド行として解釈する。[_**MUST**_]
  - 全てのコマンドと定義番号は**Case-Insensitive**である。すなわち、定義番号`AA`と`aa`は**区別しない**。[_**MUST**_]
- ファイル内で数値を解釈する必要がある場合、原則として**倍精度(64bit)浮動小数点数**相当の精度で処理する。
  - 一般的な指数表現(例:`1.2e30`)を正しく解釈しなければならない。
  - 十進表現以外(例:`0xff`)は受け付けなくてもよい。数値を期待するコマンドでそのような記述があった場合、解釈器は以下のいずれかで処理する。
    - その行全体を無視する。
    - その値を`0`として扱う。
    - `0`が許されないコマンドについては、適当なデフォルト値に置き換える。
  - 整数値が期待されている状況では、解釈器はその数値の小数点以下を**切り捨て**てもよい。**四捨五入はしない。**[_**MUST**_]
  - 倍精度浮動小数点数の範囲を超える記述(例:`1e500`)がある場合、解釈器はそれを無限(`Infinity`など)と見なしてもよい。
  - 倍精度浮動小数点数の精度を超える記述(例:`1.2345678901234567890123`)がある場合、解釈器はその桁数を適当に切り捨ててもよい。
  - 解釈器はこれより高い精度で数値を処理してもよいが、ファイル作成者はそれを期待してはならない。
- 特殊記法として**分数形式**をサポートする。[_**MUST**_]
  - 分数形式は`a/b`の形で記述される。
    - `a`と`b`はそれぞれ上述の浮動小数点数表現である。
    - `/`の前後には任意数の空白文字を挟んでもよい。
  - 解釈器は、その値を**内部的にも分数として保持する必要はない**。
    - 解釈器は値`a/b`を、数式`a/b`の演算結果であるような浮動小数点数として解釈してよい。
  - 例
    - `1/2`: `0.5`に等しい値として解釈する。
    - `1/3`: 倍精度において`0.33333333333333331482961..`に等しい値として解釈する。
      - この解釈では`1/3 * 3000`が厳密に`1000`と等しくならない場合もあるが、譜面作者はその誤差を許容しなければならない。なお倍精度において`1/3 * 3000`と`1000`との差は`10^-11`程度である。
      - 解釈器がエディターである場合、ファイルの保存時には可能な限り`1/3`という表記を復元する。[_**SHOULD**_]
    - `1/0`: `+Infinity`または不正な表現として処理する。
    - `0/0`: 不正な表現として処理する。または、このコマンド全体を無視する。
- ファイルの内容はファイル先頭から順次解釈する。[_**MUST**_]
  - 競合するコマンドが複数見つかった場合、コマンドの種類によって以下を採用する。[_**MUST**_]
    - 定義コマンド及びメタコマンド: 最後に書かれているものを採用する(出現する度に上書きされる)。
    - シーケンスコマンド及び分岐コマンド: 記述された順に全てを適用(採用)する。

## 添付されるファイルについて
- BMSTそれ自体は単なる楽譜であり、音楽を構成するためには音声ファイルを同梱する必要がある。またゲームとしてプレイする際、それに華を添える映像や画像類を同梱する余地がある。
- 解釈器は、画像ファイルとして最低でも`.png` `.jpg`に対応する。[_**MUST**_]
  -  `.bmp`, `.gif`, `.tif`などその他のビットマップ形式に対応してもよい。
  - `.svg`のようなベクター形式に対応してもよいが、ファイル作成者は解釈器がそれらに対応していることを期待してはならない。
- 解釈器は、音声ファイルとして最低でも`.wav`(PCM) `.ogg`に対応する。[_**MUST**_]
  - `.wav`(PCM)においては少なくとも以下の形式に対応する。
    - チャンネル数(_**MUST**_): `1` `2`
    - サンプリング周波数(_**MUST**_): `22050` `44100` `48000`
    - 量子化ビット数(_**MUST**_): `unsigned 8bit` `signed 16bit`
    - 量子化ビット数(_**SHOULD**_): `signed 24bit` `32bit float`
  - `.wav`(μ-law)や`.mp3`のようなその他の音声形式に対応してもよい。
- 解釈器は、動画ファイルとして最低でも`.mp4`(h264)に対応する。[_**MUST**_]
  - 可能な限り`.mp4`(hevc)にも対応する。[_**SHOULD**_]
  - `.wmv`や`.av1`などその他の動画形式に対応してもよいが、動画作者は解釈器がそれらに対応していることを期待してはならない。
- ファイル名を指定するコマンドについて
  - 解釈器は、少なくとも**下方向の相対パス**に対応する。[_**MUST**_]
    - 例: `#JACKET img/jacket.png` と書かれている場合、この`.bmst`ファイルと同じフォルダに`img`というフォルダが存在すれば、その中にある`jacket.png`をジャケット画像として採用する。
  - 解釈器は、この値が**URL**として解釈できる場合、オンラインでの取得を試みてもよい。[_**MAY**_]
    - ファイル作成者は、そのURLが利用可能かつ安全であることについて責任を負わなければならない。
  - 解釈器は、記述されたものと全く同じ名前のファイルが見つからない場合、**拡張子に限り**任意の形式に変換して探し出してもよい。[_**SHOULD**_]
    - 例えば`#JACKET a.png`と記述されているにも関わらず`a.png`が存在せず、代わりに`a.bmp`が存在している場合には、解釈器は`a.bmp`を実際の`#JACKET`として採用できる。
    - どの拡張子を互換とするかは解釈器の自由である。`a.png`の代替として`a.wav`を探してもよい(それが画像として解釈できるかは別として)。
    - 上記例において`a.png`が実際に存在する場合は、解釈器は必ず`a.png`を採用する。`a.bmp`を用いてはならない。[_**MUST**_]
  - 解釈器は、記述されたファイルが見つからないか読み込めない場合、**空白が指定されている**とみなして処理する。
    - `#DEFSOUND`(音声定義)においては、無音の音声ファイルと同様の処理になる。
    - `#DEFIMAGE`(画像定義)においては、透明の画像ファイルと同様の処理になる。

# コマンド
- 1個のコマンドが1行を構成する。[_**MUST**_]
  - コマンドの途中で改行は許されない。途中で改行されている場合、2行目以降はコメント扱いになる。
  - 1行に複数のコマンドは許されない。行の途中に出現する`#`は、単に文字`#`として扱われる。
- コマンドには以下の種類がある。
  - メタコマンド
  - 定義コマンド
  - シーケンスコマンド
  - 分岐コマンド
- コマンド行は正規表現 `^\s*#([0-9A-Z_]+)\s+(.+)$` にマッチする形式である。[_**MUST**_]
  - `^`: 行頭
  - `\s*`: 0個以上の空白文字
  - `#`: コマンドの開始
  - グループ1: コマンド識別子(タグ)
    - `[0-9A-Z_]+`: 1個以上のASCII単語文字
    - 半角英数とアンダースコア`_`が「単語文字」に含まれる。
      - 識別子の大文字と小文字は区別しないことに注意。
      - アンダースコアを除く記号類(例:`-` `*`)や非ASCII文字(例:漢字)は識別子に使用できない。
    - 識別子に空白を含めることはできない。識別子内で単語を区切る場合はアンダースコア`_`を使用する。
  - `\s+`: 1個以上の空白文字
    - Unicodeで空白文字として定義されているもの(例:`U+0020`(半角スペース) `U+0009`(水平タブ) `U+3000`(全角スペース))。
    - これ以外の文字が出現した時点で、それ以降がコマンドの内容となる。
  - グループ2: コマンドの内容
    - `.+`: 1個以上の任意の文字
    - 記号、空白を含む任意の文字種にマッチする。
    - コマンド内容を空白文字で始めることはできないが、空白文字で終わることはできる。
      - `#HOGE abc`と`#HOGE  abc`(間の半角スペースが2個)は全く同じもの(`abc`)として扱われるが、`#HOGE abc`と`#HOGE abc `(最後に半角スペースがある)は異なるものとして扱われる可能性がある。
      - `#Title`のような、内容をそのまま文字列として受け取るコマンドでは特に注意。
    - コマンドによって、実際に使用(解釈)可能な文字種は更に制限される。
    - 例えば、数値を要求するタグにおいては、値の前後の空白を削除した上で、残った部分を数値として解釈するよう試みる。
  - `$`: 行末
- 例: `  #HOGE  fuga  piyo  `(行頭、行末、及び各単語間に半角スペースが2個ずつ)
  - 識別子(コマンド名)は`HOGE`である。
    - コマンド行を表す`#`より前にある空白は全て無視される。
    - これは`#hoge`と書かれたコマンドと同一である。
  - コマンドの内容は`fuga  piyo  `である。
    - 識別子`HOGE`と内容の始まり`fuga`の間にある空白は全て無視される。
    - `piyo`の**後ろにある空白は、全てコマンドの内容に含まれる**。
- 「`#`とその後の最初の空白文字(または行末)との間」がコマンドの識別子である。コマンドの識別子と内容は必ず空白で区切られなければならない。[_**MUST**_]
  - 例: `#ABC EX1`は、コマンド識別子が`ABC`で内容が`EX1`である。
  - 例: `#ABCEX 1`は、コマンド識別子が`ABCEX`で内容が`1`である。
  - 上記の2例は全く異なるコマンドであって、`#ABC`の一致だけをもって後者を`ABC`コマンドと見なしてはならない。
- この文書では可読性を考慮し、コマンドの識別子に小文字を交えて表記する場合もあるが、実際のファイルに記述する際は全て大文字または全て小文字でも構わない。

## メタコマンド
- BMSTのメタ情報を記述するためのコマンド。
- 後述する定義コマンド、シーケンスコマンド、分岐コマンドのいずれでもないものは全てメタコマンドである。
- メタコマンドの内容は**分岐の決定前**(後述)に確定する。[_**SHOULD**_]
  - 解釈器は、メタコマンド以外のコマンドが出現した場合、それ以降のメタコマンドを全て無視してもよい。[_**MAY**_]
    - すなわち、解釈器は、ファイルの全体を読み込むのではなく、冒頭の一部分だけを読み込んで、タイトル等のメタ情報を確定してよい。
    - これは読み込み負荷を軽減するための仕様である。
  - ファイル作成者は、ファイルの冒頭にはメタコマンド(及びコメント)のみを記述し、その他の種類のコマンドは全てのメタコマンドより後に記述しなければならない。
- 同じタグのメタコマンドが複数記述されている場合、**最後に記述されているもののみ**を採用する。[_**MUST**_]
- 分岐コマンド(後述)の内側にメタコマンドが見つかった場合、解釈器は**以下のいずれか**を採用する。[_**MUST**_]
  - 分岐内のメタコマンドは無視する。
    - 上記「それ以降のメタコマンドを全て無視」する場合は必然的にこちらになる。
  - 分岐内のメタコマンドは全てトップレベルに記述されているとみなす。
  - いずれにせよ、**メタコマンドがランダムに適用されることはない**。そのようなBMSTは作成できない。
  - 譜面作者は、分岐内にメタコマンドを記述してはならない。

### 既定のメタコマンド
解釈器は少なくとも以下の全てのメタコマンドを解釈できなければならない[_**MUST**_]。その他独自実装のメタコマンドを採用してもよい。
- `#Genre`
  - 例: `#Genre TRANCE`
  - 楽曲のジャンルを表す文字列。
  - 「ジャンル」の定義は規定しない。
- `#Title`
  - 例: `#Title たのしいピクニック`
  - 楽曲のタイトルを表す文字列。
  - **楽曲の識別子**としても使われる。ファイル作成者は**このタグを省略してはならない**し、**この値を空白にしてはならない**。
- `#SubTitle`
  - 例: `#SubTitle (Radio Edit)`
  - 楽曲の副題を表す文字列。
  - **`SUB`と`TITLE`の間に区切り文字は入れない**。
  - 解釈器はこの値に対して自動的に括弧を付与**しない**[_**MUST**_]。副題を括弧で囲う必要がある場合、ファイル作成者は実際に記述しなければならない。
  - `#SubTitle`が存在せず、`#Title`が括弧付きの表現で終わる場合、解釈器はその部分を`#SubTitle`と同等に扱ってもよい。[_**MAY**_]
- `#Artist`
  - 例: `#Artist Andrew Helio`
  - この楽曲の作者を表す文字列。
  - 「アーティスト」の定義は既定しない。
- `#SubArtist`
  - 例: `#SubArtist John Doe`
  - この楽曲や譜面に関わった人物を表す文字列。
  - BMSではこのタグを複数記述することで複数人のサブ作者を表すこともあったが、BMSTにおいて**同じタグを複数記述することはできない**(最後の一つしか適用されない)。
  - 複数人のサブ作者がいる場合は、`/`で区切るなどして全てを一つの`#SubArtist`に記述する。
- `#BPM`
  - 例: `#BPM 160`
  - この譜面の**開始時**のテンポを表す**数値**。
  - 解釈器は、少なくとも有効数字6桁の精度(`100.000`と`100.001`の区別)を保証する。[_**MUST**_]
  - 解釈器は、このタグが存在しないか、この値が`0`より大きい有限数として解釈できない場合、**そのBMSTを不正なものとみなしてもよい**。[_**MAY**_]
    - 有効なBMSTとみなす場合、デフォルト値として`120`を採用する。[_**MUST**_]
- `#Level`
  - 例: `#Level 10`
  - この譜面の難易度を表す**数値**。
  - BMSの`#PLAYLEVEL`に相当する。
  - 解釈器は、非整数や非数値の`#Level`を想定しなければならない。[_**MUST**_]
    - ソート時などレベルを数値として解釈しなければならない場合、非数値の`#Level`を一律に`正または負の無限大`とみなしてもよい。[_**MAY**_]
    - すなわち、レベル順でソートする際、非数値の`#Level`はリストの最初か最後にまとめて配置する。
  - 解釈器は、このタグが省略された場合はデフォルト値として`1`を採用する。[_**MUST**_]
- `#Difficulty`
  - 例: `#Difficulty 3`
  - この譜面の難易度区分を表す**数値**。
  - `#Level`とは別軸の評価である。
    - ファイル作成者は、いずれかを省略する場合、`#Level`を優先的に残すべきである。
  - 解釈器は、非整数や非数値の`#Difficulty`を想定しなければならない。[_**MUST**_]
    - ソート時など難易度区分を数値として解釈しなければならない場合、非数値の`#Difficulty`を一律に`正または負の無限大`とみなしてもよい。[_**MAY**_]
  - このタグが省略された場合、デフォルト値として推奨される値は`3`。[_**SHOULD**_]
- `#JudgeRank`
  - 例: `#JudgeRank 3`
  - この譜面の判定の厳しさを表す文字列。
  - BMSの`#RANK`に相当する。
  - 解釈器は少なくとも以下の値を受容する。[_**MUST**_]
    - `0`: Very Hard (最も厳しい判定幅)
    - `1`: Hard (厳しい判定幅)
    - `2`: Normal (通常の判定幅)
    - `3`: Easy (緩い判定幅)
    - 各値における具体的な判定幅は解釈器が独自に設定してよい。
    - これ以外の値や数値以外(`#JudgeRank Easy`など)を受容してもよい。
  - このタグが省略された場合、解釈器は独自に設定したデフォルト値を適用する。
  - 未定義の値が記述されている場合、このコマンド自体を無視し、省略されている場合と同じように扱う。
- `#JudgeWidth`
  - 例: `#JudgeWidth 1.25`
  - この譜面における成功判定幅の補正を表す**数値**。
  - `#JudgeRank`から規定される判定幅に、ここで指定された値をかけたものが実際のプレイにおける成功判定の幅となる。
    - 例: 元々の最高判定幅が`16ms`で`#JudgeWidth 1.25`と記述されている場合、実際の最高判定の幅は`16*1.25 = 20ms`となる。
  - 失敗判定及び見逃し判定(後述)の幅は変更しないものとする[_**SHOULD**_]。変更してもよい。
- `#Recover`
  - 例: `#Recover 0.1`
  - この譜面の**ノート1個を**最高判定で処理した場合に回復するゲージの量を表す**数値**。
  - BMSの`#TOTAL`に相当するが、あちらは全ノートにおける総量を記述するため、全く異なる数値になる。
  - 通常のプレイモード(ノーマルゲージ)におけるゲージ上限値は`100`を想定する。
    - `#Recover 0.1`の場合「1000個のノートを最高判定で処理するとゲージが100%になる」と期待される。
  - 解釈器は、このタグが存在しないか、この値が`0`以上の有限数として解釈できない場合、任意に(決定論的に)算出した値を`#TOTAL`の代替としてもよい。[_**SHOULD**_]
- `#LnMode`
  - 例: `#LnMode 1`
  - この譜面に含まれる**ロングノートの処理方法**を規定する値。
  - 解釈器は少なくとも以下の値を受容する。[_**MUST**_]
    - `0`または未指定: Auto
      - ゲームとしての解釈器は、実際のプレイの際、任意のロングノートモードを採用してよい。
    - `1`: 1個のノート (LN)
    - `2`: 2個のノート (CN)
    - `3`: 2個のノート+持続判定 (HCN)
    - ゲームとしての実装規定は「ロングノートモード」の項目を参照。
    - これ以外の値や数値以外(`#LnMode CN`など)を受容してもよい。
  - 未定義の値が記述されている場合は未指定(`0`)として扱う。
- `#Volume`
  - 例: `#Volume 0.7`
  - 全体の音量倍率を規定する**数値**。
  - 元々の音量を`1`とした割合で指定する。
  - `0`未満の値は`0`に矯正する。[_**MUST**_]
  - 解釈器は`1`より大きい値を`1`に矯正してもよい。
  - 未指定または数値として解釈できない場合は`1`とみなす。[_**MUST**_]
- `#BgaSize`
  - 例: `#BgaSize 1280,720`
  - `#DefImage`における表示先矩形を指定する際の基準となる、BGA表示領域のサイズ。
  - `(横), (縦)`の順に`,`で区切って**ピクセル単位**で指定する。
    - 小数点以下は無視する。
    - 数値が1つだけの場合、正方形の一辺を指定しているものとみなす。
    - 数値が3つ以上の場合、3つ目以降は無視する。
    - 縦横いずれかが数値として解釈できない場合は未指定とみなす。
  - 解釈器が画面に表示する実際の大きさは、**縦横比を保ったまま**適当に拡縮してよい。
  - 未指定の場合、解釈器は以下のいずれかの方法でデフォルト値を設定する。
    - 最初に配置された映像のサイズを`#BgaSize`とみなす。[_**SHOULD**_]
    - 解釈器が任意に設定したデフォルト値を`#BgaSize`として適用する。
- `#BgaOverflow`
  - 例: `#BgaOverflow 0`
  - `#DefImage`における表示先矩形が`#BgaSize`の範囲外となった際の表示方法を指定する**数値**。
  - 解釈器は少なくとも以下の値を受容する。[_**MUST**_]
    - `0`または未指定: Auto
      - 解釈器の実装に委ねる。
      - 常にクリップを適用してもよいし、実際の配置から自動的に切り替えてもよい。
    - `1`: クリップ
      - 範囲外の部分は全て非表示とする。
      - BGAはBGAエリアをはみ出ることがない。
    - `2`: オーバーフロー
      - 範囲外の部分も全て描画する。
      - 演奏ノートよりは下に描画するが、その他のスキン要素との関係は解釈器またはスキン設定に依存する。
      - BGAを最前面に表示する実装の場合は演奏ノートが隠れる可能性もある。
  - 未定義の値が記述されている場合は未指定(`0`)として扱う。
- `#AutoResol`
  - 例: `#AutoResol 1/64`
  - オートメーションタイプのシーケンスコマンド(後述)における、オートメーションの適用間隔を表す**数値**。
  - 音価単位(全音符=`1`, 四分音符=`1/4`)で指定する。例では分数形式だが小数形式も可能。
  - 未指定または`0`より大きい有限値として解釈できない場合はデフォルト値として`1/32`を採用する。[_**MUST**_]
- `#Jacket`
  - 例: `#Jacket _jacket.png`
  - この譜面のジャケット画像の**ファイル名**。
  - BMSの`#STAGEFILE`に相当する。
  - 動画ファイル(`.gif`や`.mp4`など)が指定されている場合、解釈器は可能ならそれを動画として表示する。[_**SHOULD**_]
    - 動画としての表示が困難な場合、解釈器はその最初のフレームを静止画として表示する。[_**MUST**_]
    - 以降の画像系ヘッダーコマンドでも同様である。
- `#Banner`
  - 例: `#Banner _banner.png`
  - この譜面のバナー画像の**ファイル名**。
  - 解釈器は、`#Jacket`と`#Banner`が両方記述されている場合、そのどちら(あるいは両方)を表示するか任意に(決定論的に)決めてよい。
- `#Loading`
  - 例: `#Loading _title.png`
  - この譜面を実際にプレイする際、ロード中に表示される画像の**ファイル名**。
  - BMSの`#BACKBMP`のRED BELT系スキンでの使用方法に相当する。
  - 解釈器は、実際にロード中に表示する場合、それがフォントによるタイトル表示の代わりであることを想定しなければならない。[_**MUST**_]
    - 例: フォントによるジャンル/タイトル/作者を表示せず、代わりにその場所にこの画像を表示する。
- `#Ready`
  - 例: `#Ready _ready.png`
  - プレイ前のロードが完了し、演奏が開始される直前に表示される画像の**ファイル名**。
    - 指定されている場合、ロード完了の時点で`#Loading`の画像は消去する。
    - 未指定の場合は`#Loading`の画像をそのまま表示し続ける。
  - 動画が指定されている場合(または`#Ready`が未指定で`#Loading`に動画が指定されている場合)、その再生が1周分終わるまでプレイ開始を遅らせる。[_**MUST**_]
- `#Preview`
  - 例: `#Preview _preview.wav`
  - この譜面の試聴用音源の**ファイル名**。
  - 解釈器は、選曲画面等でその曲の試聴音声を流す場合、`#Preview`で指定された音声ファイルが存在すれば**必ずその音声を再生する**。[_**MUST**_]
    - 実際にBMSTを再生した際の音声とは全く異なる内容となる可能性もある。`#Preview`が試聴として正しく機能するかどうかは**ファイル作成者が責任を持つ**。
  - このタグが存在しないか、存在しても再生可能なファイルが指定されていない場合は、シーケンス内容から音声を生成して再生してもよい。

## 定義コマンド
- BMSTの譜面データで使用する音声などを登録するためのコマンド群。
- 定義コマンドは`#DEF`で始まり、その他の種類のコマンドは`#DEF`では始まらない。
  - 解釈器は、行が`#DEF`で始まる場合、常にそれを定義コマンドとみなしてよい。
  - 独自拡張のコマンドを作成する際も、この規則に従わなければならない。
- コマンドの内容は正規表現 `([0-9A-Z]+)(?:\s+(.*))?` にマッチする形式である。
  - グループ1: 定義番号
    - `[0-9A-Z]+`: 1個以上のASCII英数字
    - 定義番号は三十六進表現で記述された正の整数である。
      - **`0`は利用できない**。定義番号`0`は常に空白を表すために使われる。
      - `#DefSound 0 aaa.wav`などと記述してもエラーにはならないが、決して参照されない。
    - 解釈器は先頭の連続する`0`を許容し、数値としては無視する。[_**MUST**_]
      - 例: `010`は`10`と等しく、いずれも十進表現で`36`に相当する有効な定義番号である。
      - 例: `A`は`00A`と等しく、いずれも十進表現で`10`に相当する有効な定義番号である。
    - 定義番号における大文字と小文字は**常に区別しない**。[_**MUST**_]
      - 例: `1A`と`1a`は等しく、いずれも十進表現で`46`に相当する定義番号である。
    - 定義番号に使用できない文字が出現した場合、その行は不正な定義コマンドとみなし、エラーとするか無視する。[_**MUST**_]
    - 定義番号の上限は`ZIK0ZJ`(十進表現で`2147483647`)
      - 解釈器は、定義番号を32bit符号付き整数(またはその上位互換)で管理する。[_**MUST**_]
      - この制限は以下の余地を生む。
        - 定義番号は単なる番号ではなく「6文字以下の英単語(例:`apple`)」とすることもできる。
        - 十進表現(例:`12345`)に限っても約100万種類の定義番号が利用可能。
      - 定義番号が上限を超える場合、その行は不正な定義コマンドとみなし、エラーとするか無視する。[_**MUST**_]
      - 解釈器は`ZZZZZZ`(十進表現で`2176782335`)をサポートしてもよいが、ファイル作成者はそれを期待してはならない。
  - 以下は省略可能。
    - `\s+`: 1個以上の空白文字 
      - これ以外の文字が出現した時点で、それ以降が定義内容となる。
      - 空白は省略できない。例えば`#DefSound 001abc.wav`は「定義番号`001abc.wav`で内容が空の定義」と解釈されるが、そのような定義番号は許されないため、結局このコマンド全体が無視される。
    - グループ2: 定義内容
      - `.*`: 0個以上の任意の文字
  - 定義内容は空にできる。この場合は原則として**未定義と同じ扱い**とする。
    - 定義内容が空の場合、コマンドの内容は定義番号のみになる。
    - 例: `#DefSound 001`は有効なコマンドであり、定義番号`001`の内容が空であることを示す。
    - ファイルを指定する際は拡張子まで記述するべきだが、省略することもできる。解釈器は、定義の拡張子が省略されている場合、適当に補完してもよい。
- 同じ定義番号の定義コマンドが複数記述されている場合、**最後に記述されているもののみ**を採用する。[_**MUST**_]
- 分岐コマンド(後述)の内側に定義コマンドが見つかった場合、解釈器は**以下のいずれか**を採用する。[_**MUST**_]
  - 分岐内の定義コマンドは無視する。
  - 分岐内の定義コマンドは全てトップレベルに記述されているとみなす。
  - いずれにせよ、定義コマンドが**ランダムに適用されることはない**。そのようなBMSTは作成できない。
  - 譜面作者は、分岐内に定義コマンドを記述してはならない。

### 規定の定義コマンド
解釈器は少なくとも以下の全ての定義コマンドを解釈できなければならない[_**MUST**_]。その他独自実装の定義コマンドを採用してもよい。
- `#DefSound`
  - 例: `#DefSound 001 bgm_00.wav`
  - BMSTで使用する音声ファイルを登録する。
  - 以下のいずれかの場合、その番号は空定義とみなす。[_**MUST**_]
    - 定義内容がファイルパスまたはURLとして有効な文字列ではない場合(空白文字しかない場合も含む)
    - 定義内容に該当するファイルが存在しない、または音声ファイルとして解釈できない場合
  - パスとして使用できない文字はシステムや解釈器に依存するが、`|`は区切り文字として扱い、常にパスに使用できない。
    - 解釈器は、定義内容に`|`が含まれる場合、その直前までをファイル名として採用する。[_**MUST**_]
  - 詳細指定: ファイルパスの後に`|`で区切って詳細指定を行うことができる。
    - 例: `#DefSound 001 bgm_00.wav| start=16.3 len=25`
      - `bgm_00.wav`という音声ファイルを参照する。
      - 音声は`16.3`秒の時点から`25`秒間(`41.3`秒時点まで)再生される。
    - 例: `#DefSound 002 |ref=001 vol=0.5`
      - `#DefSound 001`で定義されたものと同じ音声ファイルを参照する。
      - 音声の再生区間等は`#DefSound 001`で指定されたものを引き継ぐ。
      - 再生時には音量を`0.5`倍(約`-6dB`)にする。
    - 詳細指定は`(名前)=(値)`の形で記述し、`,`か空白文字で区切る。`=`の前後には空白は入れない。
    - 詳細指定では、音声の再生時のオプションを指定できる。
    - 値は基本的に数値として解釈される。
      - 正常な値として解釈できない場合、その指定は無視する。[_**MUST**_]
    - 解釈器は、音声の再生機能を持たない場合、詳細指定を全て無視してもよい。
  - 既定の詳細指定は下記のとおり。[_**MUST**_]表記の無い詳細指定は対応しなくてもよい。
    - `ref`: 定義参照[_**MUST**_]
      - 指定された番号の定義内容を継承する。
      - ファイル名と全ての詳細指定を継承する。
      - この定義で新たに指定されているものは上書きされる。
        - ファイル名については、空(空白文字のみ)の場合は継承された値をそのまま使う。
        - ファイル名が空でない場合、ファイル名はこの定義に記述されているもので上書きする。
      - 継承は再帰的に行う。継承元で`ref`が指定されていればそれも継承する。
      - 参照元にできる定義番号は、この定義より前に書かれているものに限る。
        - **より若い番号という意味ではない。**`#DefSound 002`が`#DefSound 001`よりも上に記述されている場合、`#DefSound 001 |ref=002`は有効。
        - 未定義の番号を参照した場合、単に`ref`が無い場合と同じように扱う。
    - `start`: 再生開始位置[_**MUST**_]
      - `0`以上の秒数単位で再生開始位置を指定する。
      - `0`未満の場合、`0`に矯正する。
      - 指定値が元々の再生時間以上の場合、その音声は再生しない。
    - `len`: 再生時間[_**MUST**_]
      - `0`以上の秒数単位で再生時間の長さを指定する。
      - 指定値が`0`未満、または再生開始位置との合計が元々の再生時間以上の場合、音声の最後まで再生する。
    - `vol`: 音量[_**MUST**_]
      - 元々の音声の音量を`1`とした割合で指定する。
      - この音声を再生する際、波形全体にこの指定値を掛けてから再生する。
    - `pan`: 定位
      - `-1`から`1`までの間で指定する。
      - `0`より小さい場合、その値に応じて元の音声の**右側の音量のみ**を減少させる。
        - `-0.5`で元の音量の半分(左寄りに聞こえる)、`-1`で完全なゼロ(左だけ聞こえる)になる。
      - `0`より大きい場合、その値に応じて元の音声の**左側の音量のみ**を減少させる。
        - `0.5`で元の音量の半分(右寄りに聞こえる)、`1`で完全なゼロ(右だけ聞こえる)になる。
      - ファイル作成者は、解釈器がこの指定を無視する可能性を考慮しなければならない。
    - `pitch`: ピッチ
      - cent単位で再生速度の変更率を指定する。
      - `-1200`で元の半分の速度、`1200`で元の2倍の速度で再生する。
      - ファイル作成者は、解釈器がこの指定を無視する可能性を考慮しなければならない。
- `#DefImage`
  - 例: `#DefImage 001 bga.mp4`
  - BMSTで使用する画像/動画ファイルを登録する。
    - この定義はBGAでの使用を前提としている。**動画ファイルは音声を無視する**。[_**MUST**_]
  - コマンド内容の基本事項は`#DefSound`と同様。
    - 詳細指定: ファイルパスの後に`|`で区切って詳細指定を行うことができる。
    - 例: `#DefImage 001 bga.mp4| sx=0 sy=20 sw=256 sh=256 dx=50 dy=100`
    - 基本事項は`#DefSound`と同様。
    - 詳細指定では、映像の表示時のオプションを指定できる。
    - 解釈器は、映像の表示機能を持たない場合、詳細指定を全て無視してもよい。
  - 既定の詳細指定は下記のとおり。[_**MUST**_]表記の無い詳細指定は実装しなくてもよい。
    - `ref`: 定義参照[_**MUST**_]
      - 例: `#DefImage 002 |ref=001 sx=400`
      - 指定された番号の定義内容を継承する。
      - 詳細は`#DefSound |ref`を参照。
    - `sx` `sy` `sw` `sh`: 参照範囲[_**MUST**_]
      - 元の画像(動画)から切り抜く範囲を**ピクセル単位**で指定する。切り抜き範囲の左上の座標を`sx`と`sy`で、切り抜く幅を`sw`、高さを`sh`で指定する。
      - 値が元画像の範囲を超える場合、元範囲と重なる部分だけに矯正する。
      - 未指定の場合、`sx=0` `sy=0` `sw=(元画像の幅)` `sh=(元画像の高さ)`とみなす。
    - `dx` `dy`: 描画先座標[_**MUST**_]
      - BGAエリアに描画する際のオフセットを**ピクセル単位**で指定する。
      - BGAエリアの幅と高さ`#BgaSize`で指定する。
      - 未指定の場合、`dx=0` `dy=0`とみなす。
    - `dw` `dh`: 描画先サイズ[_**MUST**_]
      - BGAエリアに描画する際のサイズを**ピクセル単位**で指定する。
      - `sw`や`sh`と異なる値が指定された場合は、縦横それぞれを個別に拡縮する。
      - 未指定の場合、`dw=(sw)` `dh=(sh)`とみなす。
      - 描画先の座標及びサイズが`#BgaSize`の領域からはみ出る場合は`#BgaOverflow`での指定に従って処理する。
    - `start`: 再生開始位置
      - `0`以上の秒数単位で再生開始位置を指定する。
      - 対象のファイルが動画である場合にのみ有効。
      - 解釈器はこの指定を無視してもよい。その場合、動画は常に最初から再生される。
      - 指定値が元々の再生時間以上の場合、(表示しないのではなく)**最後のフレーム**を表示する。
      - ファイル作成者は、解釈器がこの指定を無視する可能性を考慮しなければならない。
    - `len`: 再生時間[_**MUST**_]
      - `0`以上の秒数単位で再生時間の長さを指定する。
      - 対象のファイルが動画である場合にのみ有効。
      - 再生開始位置との合計が元々の再生時間より短い場合、その時点のフレームを最後のフレームとして扱う。
      - 例: `start=10 len=10`
        - 元の動画の長さが20秒以下の場合、再生終了後は元々の最後のフレームが表示され続ける。
        - 元の動画の長さが20秒より長い場合、**20秒目のフレームで再生を停止**し、そのフレームを表示し続ける。
- `#DefSequence`
  - 例: `#DefSequence S01 1,2,3,4 | len=1.5`
  - 複数の番号の並びをグループ化して参照するための特殊な定義を登録する。
    - 音声チャンネル(演奏レーンやBGMレーンなど)においては、複数の音声を連続して再生する挙動になる。
    - 映像チャンネル(BGAレーンなど)においては、複数の画像/動画を連続して表示する挙動になる。
  - **定義番号列タイプのシーケンスコマンド**(後述)で参照できる。
  - 定義内容は**定義番号列タイプのシーケンス**(後述)と同じ記法で記述する。
  - 実際にどの定義が参照されるかはチャンネルによって変わる。
    - 音声チャンネルにおいては`#DefSound`の定義番号が参照される。
    - 映像チャンネルにおいては`#DefImage`の定義番号が参照される。
  - **`#DefSequence`の中で`#DefSequence`の定義番号は参照できない**。
    - 例: `#DefSequence 01 1,0,1,0`において、`1,0,1,0`の部分で参照される定義番号`1`は**元の定義(`#DefSound`など)における番号**であり、それが未定義であれば無音(あるいは透明)になる。
  - 定義番号の空間は**元となる定義(`#DefSound`など)と共有する**。
    - 該当の番号が元々定義されている場合は**常にそちらが優先される**。`#DefSequence`はその番号が未定義または空白の場合に限り参照される。
    - 例: `#DefSound A1 hoge.wav`と`#DefSequence A1`が両方存在する場合、`A1`は`#DefSound A1`を参照し、**`#DefSequence A1`は参照されない**。
    - 例: `#DefSound A1`(空の定義)と`#DefSequence A1`が存在する場合、`A1`は **`#DefSequence A1`を参照する**。`#DefSound A1`が存在しない場合も同じ。
  - `#DefSequence`を参照するとしても、配置されたノートは**1個のノートとして扱う**。
    - 音声チャンネルにおいては、これは単純に複数の音声を連結した音声のように振る舞う。
    - 映像チャンネルにおいては、これはパラパラ漫画形式のシーケンス(いわゆる連番BMP)の繰り返しを効率的に記述するために使用される。
  - 詳細指定: シーケンス記述の後に`|`で区切って詳細指定を行うことができる。
    - `len`: シーケンスの長さ
      - シーケンス部分の全体長を**音価単位**(全音符=`1`, 四分音符=`1/4`)で指定する。
      - **未指定の場合は`1`とみなす**。
      - このシーケンスが配置される小節の元々の長さとは無関係に適用される。
- `#DefVar`
  - 例: `#DefVar 001 rand(3)`
  - 分岐コマンド(後述)で使用する条件用の変数を登録する。
    - 定義内容は**数式**である(詳細は後述)。基本的には以下のいずれかの形式で記述する。
      - 定数値(例: `3`): その数値をそのまま変数の値とする。小数/分数表現も可能。
      - 乱数値(例: `rand(3)`): `0`以上で指定値**未満**のランダムな整数を変数の値とする。数値は0より大きい整数でなければならない。
    - 不正な表現は全て`0`とみなす。
  - 変数の内容はファイルの読み込み時点では評価(計算)せず、**実際にその値が必要になった時に初めて評価する**。[_**MUST**_]
    - 典型的には、プレイのために楽曲データを読み込む際に、分岐を決定するために評価することになる。
    - 数式の正常性チェックはファイルの読み込み時点で行ってよい。
    - どこからも参照されていない変数の内容は最後まで評価されない。
    - 一度評価された変数は**再評価しない**。`rand`の値は最初に参照した時点で決定し、その後は何度参照しても同じ値となる。
  - 変数は数式において`$`と定義番号を続けることで参照できる。
    - 例: `$1`: 定義番号`001`の変数に置換される。
    - 未定義の変数を参照した場合、その値は`0`とみなされる。
- `#DefExpr`
  - 例: `#DefExpr 001 cosc(t)`
  - オートメーションタイプのシーケンスコマンド(後述)で使用する、値の変化式を登録する。
    - 定義内容は**数式**である(詳細は後述)。
    - 数式中には`$001`の形で`#DefVar`で定義した変数を使用できる。
    - 数式中の`t`には変化率に応じて`0`から`1`の値が入る。
    - `t`及び`#DefVar`への参照を除く変数(例:`x`など)は利用できない。そのような「未知の値」は**全て`0`とみなす**。[_**MUST**_]
  - 変数定義`#DefVar`と異なり、`#DefExpr`は参照される度に評価される。

## シーケンスコマンド
- BMSTの譜面データ本体を記述するためのコマンド。
- シーケンスコマンドは数字`[0-9]`のみで構成され、その他の種類のコマンドは数字では始まらない。
  - 解釈器は、行が`#`に続く数字で始まる場合、常にそれをシーケンスコマンドとみなしてよい。
  - 独自拡張のコマンドを作成する際も、この規則に従わなければならない。
- コマンド全体は正規表現 `^\s*#(\d+)\s+([0-9A-Z]+)\s+(.+)` にマッチする形式である。
  - `^\s*#`: 行頭、0個以上の空白文字、`#`
    - その他のコマンドと同じ。
  - グループ1: 小節番号
    - `\d+`: 1個以上の数字
    - 最初の小節を`0`とした正の整数。
      - 解釈器は先頭の連続する`0`を許容し、数値としては無視する。[_**MUST**_]
      - 例: `#001 K1 2`と`#1 K1 2`は等しく、それぞれ1小節目に関するシーケンスコマンドである。
    - 上限は`65535`
      - 解釈器は`65536`以上の小節番号をサポートしてもよいが、ファイル作成者はそれを期待してはならない。
    - 小節番号`0`はセットアップ(テンポ設定やBGA開始)に使うことを想定している。ファイル作成者は**小節`0`に演奏ノートを置いてはならない**。
  - `\s+`: 1個以上の空白文字
  - グループ2: チャンネル
    - `[0-9A-Z]+`: 1個以上のASCII英数字
    - 識別子と同様、大文字と小文字は区別しない。
    - チャンネルそのものは任意の文字列にできるが、この文書でいくつかを規定する。
  - `\s+`: 1個以上の空白文字
  - グループ3: シーケンス内容
    - `.+`: 1個以上の任意の文字
    - 内容の解釈方法はチャンネルごとに変わる(後述)。
- 同じ小節で同じチャンネルのシーケンスコマンドが複数記述されている場合、**記述されている順に全てを採用する**。[_**MUST**_]
  - 同時適用が不可能なチャンネル(`Bar`など)では、最後に記述されたものだけが適用されることになる。

### 既定のチャンネル
解釈器は少なくとも以下の全てのチャンネルを解釈できなければならない[_**MUST**_]。その他独自実装のチャンネルを採用してもよい。
- `K\d+`: キー(演奏レーンの音声ノート)
  - 例: `#001 K11 01,00,01,02`
  - アルファベット`K`と、それに続く1個以上の数字
  - 数字部分はキーの番号を表す。
    - 十進法で表記する。
    - 解釈器は先頭の連続する`0`を許容し、数値としては無視する。[_**MUST**_]
      - `K001` `K01` `K1` は全て同じ番号(`1`)を意味する。
    - キーの番号と画面上の配置・物理ボタンの関係は解釈器に依存する。下記は一例。
      - 1Pスクラッチ: `K10`
      - 1P鍵盤: `K11`-`K17`
      - 2Pスクラッチ: `K20`
      - 2P鍵盤: `K21`-`K27`
  - シーケンス内容は**定義番号列タイプ**(後述)。
    - 対応する`#DefSound`の定義番号を記述する。
  - 同じチャンネルで同じ時刻に複数の演奏ノートが存在する(重なっている)場合、解釈器は以下のいずれかで処理する。[_**MUST**_]
    - 記述された順番通りに順次判定する。(多くの場合、重なっている2個目以降のノートは見逃される。)
    - 重なっているノートを全てまとめて1個のノートとして判定し、音声を鳴らす場合はその全てを同時に鳴らす。
  - 定義番号の後ろに記号を付加するとそのノートの属性を変更する。
    - 例: `1,2+,3+,4-`
    - `+`を付けるとロングノートの終端になる。ただし、以下の場合は通常ノートになる。[_**MUST**_]
      - 直前に通常ノートが存在しない場合
      - 直前の不可視でないノートがロング終端の場合
    - `-`を付けると不可視ノートになる。[_**MUST**_]
    - この例では最終的に「1拍目から2拍目にかけてロングノート、3拍目に通常ノート、4拍目に不可視ノート」というシーケンスに解釈される。
    - 記号自体は任意の個数付加できるが、`+`と`-`以外の記号が含まれる場合や`+-`を両方含む場合はいずれも通常ノートとみなす。[_**MUST**_]
- `M\d+`: 地雷(演奏レーンの地雷ノート)
  - 例: `#001 M11 3,0,5,0`
  - アルファベット`M`と、それに続く1個以上の数字
  - 数字部分はキーの番号を表す。`K`と共通。
  - シーケンス内容は**数値列タイプ**(後述)。
    - その地雷で発生するダメージ量(%)を記述する。
  - 同じチャンネルで同じ時刻に複数の地雷ノートが存在する(重なっている)場合、解釈器は記述された順番通りに順次判定する。[_**MUST**_]
    - 対応ボタンを押していた場合、合計値ぶんのダメージを受けることになる。
  - 指定値が負の場合は**ダメージではなく回復になる**。
    - 解釈器は、回復するタイプの地雷を通常(正ダメージ)の地雷と異なる外見および処理時の演出とすることが望ましい。[_**SHOULD**_]
- `B\d+`: BGM(自動で演奏される音声ノート)
  - 例: `#001 B00 05,00,05,06`
  - アルファベット`B`と、それに続く1個以上の数字
  - 数字部分はBGMレーンの番号を表すが、解釈器は全てのBGMレーンを同等に扱ってよい(全てを`B00`とみなしてもよい)。
    - 十進法で表記する。
    - 桁数は任意で、先頭の連続する`0`は全て無視する。[_**MUST**_]
  - シーケンス内容は**定義番号列タイプ**。
    - 対応する`#DefSound`の定義番号を記述する。
  - 同じチャンネルで同じ時刻に複数のBGMノートが存在する(重なっている)場合、解釈器はその全てを同時に鳴らす。[_**MUST**_]
- `V.+`: BGA(映像)
  - 例: `#001 V1 1,2,3,4,1,2,3,4`
  - アルファベット列`V`と、レイヤーを表す文字
    - `V0`または`V00`: ベースレイヤー
    - `V1`または`V01`: レイヤー1
    - `VPoor`: ミスレイヤー1
    - 解釈器は`V2`(レイヤー2)や`VPoor2`(ミスレイヤー2)などを採用してもよい。[_**MAY**_]
  - シーケンス内容は**定義番号列タイプ**。
    - 対応する`#DefImage`の定義番号を記述する。
- `Bar`: 小節長
  - 例: `#001 Bar c3/4`
  - シーケンス内容は**単一値タイプ**。
    - この小節の長さを**音価単位**(全音符=`1`, 四分音符=`1/4`)の小数または分数で記述する。
    - `4/4`のような拍子形式での記述を想定しているが、`1/3`のような拍子としては不正な表現も許容する。
  - 記法: `c?(.+)`
    - `c?`: 継続フラグ
      - 値が`c`で始まる場合、小節長の変更はその小節以降の全てに適用される。
      - 値が`c`で始まらない場合、小節長の変更はその小節のみに適用される。
    - `.+`: 小数または分数形式による数値表現
  - 値が`0`以下または無限大の場合、その小節長指定は無効(存在しないもの)とする。[_**MUST**_]
  - 小節長の初期値は`1`(=4/4拍子)である。[_**MUST**_]
- `Bpm`: テンポ変更
  - 例: `#001 Bpm 120,0,240,120`
  - シーケンス内容は**オートメーションタイプ**(後述)。
    - 実際に変更するテンポの値をBPM単位で記述する。
  - 値が`0`の場合、テンポは変化させない。
  - 値が十分に大きい場合(`Infinity`を含む)、次に通常のテンポが指定されるまでの区間が全て一瞬で処理されることになる。
  - 値が負の場合、解釈器はその時点で譜面の再生を止めてもよい。
- `Stop`: 譜面停止
  - 例: `#001 Stop 0,1/4,0,1/4`
  - シーケンス内容は**数値列タイプ**。
    - 停止時間を**音価単位**(全音符=`1`, 四分音符=`1/4`)で記述する。
  - 同じタイミングに他の種類のシーケンスが存在する場合、**`Stop`は最後に処理する**。[_**MUST**_]
    - `Bpm`がある場合は、テンポ変化を適用してから、そのテンポで計算した時間だけ停止する。
    - `K`などの音声ノートや`V`などのメタノートがある場合は、それらを処理してから停止する。
  - 値が`0`以下の場合は停止しない。
  - **負数及び非有限の値は無視される**(`0`とみなす)。譜面スキップを表現したい場合は**大きな値の`Bpm`**を使う。

解釈器は可能なら以下のチャンネルを採用する。[_**SHOULD**_]
- `Scroll`: スクロール速度変更
  - 例: `#001 Scroll 1,0,2,1`
  - シーケンス内容は**オートメーションタイプ**。
    - 本来の速度を`1`とした割合で記述する。
- `Speed`: ハイスピード変更
  - 例: `#001 Speed 1,0,2,1`
  - シーケンス内容は**オートメーションタイプ**。
    - 本来の速度を`1`とした割合で記述する。

### シーケンスの記法
- シーケンスの内容は**単一値**、**定義番号列**、**数値列**のいずれかであり、**数値列**には更に**オートメーション**が利用可能なものもある。
- **単一値タイプ**
  - 例: `#001 Bar c3/4`
  - 小節番号とチャンネルを除いた行の残りの部分が単一の値を表す。
  - **1小節を通して適用されるべき要素**に対して使う。この文書では`Bar`(小節長)での利用が想定されている。
- **定義番号列タイプ**
  - 例: `#001 K11 1,2,3,4`
  - 任意個数の定義番号の羅列。複数並べる場合は`,`で区切る。
  - 「`,`の数`+1`」が**その小節の分解能**となる。
    - `1,2,3,4`は「1小節を`4`等分(すなわち**四分音符間隔**)してノートを配置」という意味である。
  - `,`の前後には任意個数の空白文字を挿入できる。
    - `1,2,3,4`は`1, 2, 3, 4`や`1 , 2 , 3 , 4`などとも書ける。
  - `0`はその位置にノートが存在しないことを表す。`0`は省略してもよい。
    - `1,2,0,0`は「1拍目と2拍目にノートが存在し、その後は休符」という意味である。
    - `1,2,,`は上記と同じ意味になる。`,`の前後に数値が無い場合は`0`があるとみなされる。
  - 定義番号の後に`*`以外の記号(ASCII記号文字)を付けることで、そのノートに属性を付与する。
    - 演奏レーン(`K`チャンネル)においては`+`がロング終端、`-`が不可視を表す。
    - `1,0,2+,3-`は「1拍目から3拍目にかけてロングノート、4拍目に不可視ノートを配置」という意味である。
    - `K`チャンネル以外において、使用可能な記号や複数付加された場合の解釈は全て解釈器に依存する。ただし **`*`は予約されているため使用不可**。
  - 定義番号の後に`*`と数字を続けることで、**直前の番号の繰り返し**を表す。
    - `1,1,1,1`は`1*4`とも書ける。
    - `1,0,0,2`は`1,0*2,2`とも書ける。`0`を省略して`1,*2,2`とするのも**有効**。
    - `0*31,1,2,0*31`は「64分音符31個ぶんの休符の後に64分音符間隔で2個のノートを配置」という意味である。これは`*31,1,2,*31`とも書ける。
    - `*`の後ろが`1`より大きい整数値として解釈できない場合、`*`そのものを存在しないもの(繰り返し無し)として扱う。
    - 属性記号と併用する場合、**必ず`*`を最後にする**。
- **数値列タイプ**
  - 例: `#001 Stop 0, 1/4, 0, 1/4`
  - 基本的には定義番号列タイプと同じだが、定義番号の代わりに小数または分数形式の数値を直接記述する。
  - `0`は「`0`を適用」ではなく「**何もしない**」を意味する。`0`は省略してもよい。
    - 例: `#001 M11 1,0,2,0`
      - 1P1鍵に対して地雷ノートを配置するコマンド。
      - 1拍目にダメージ`1%`の地雷を配置する。
      - 3拍目にダメージ`2%`の地雷を配置する。
      - 2拍目および4拍目の`0`は「`0`ダメージの地雷を配置」**ではなく**、「地雷を配置しない」という意味である。
  - 数値表現自体に含まれる記号と繰り返しを表す`*`を除いて**いかなる記号も使用できない**。不正な記号が含まれる(数値として解釈できない)場合はその部分を`0`(存在しない)とみなす。
    - `0.1*4`は`0.1, 0.1, 0.1, 0.1`と同じ意味である。
    - `1/2, 1/2+1/2, 5e-1, -1`と書いた場合:
       - 1拍目は`1/2`=`0.5`
       - 2拍目の`1/2+1/2`は分数表現として認められないため無視(`0`とみなす)
       - 3拍目は`5e-1`=`0.5`(正しい小数形式)
       - 4拍目の`-1`は正しい小数形式だが、`Stop`チャンネルにおいては`0`とみなされる。
       - 結局これは(`Stop`では)`0.5, 0, 0.5, 0`と同じ意味である。
- **オートメーションタイプ**
  - 例: `#001 Bpm 100, $1:200`
  - 基本的には数値列タイプと同じだが、数値の前に`$[expr]:`を付加することでオートメーションを表現できる。ただし`[expr]`は対応する`#DefExpr`の定義番号。
    - 数式参照を付加されたものをここでは**オートメーションノート**と呼ぶ。そうでないものは**通常ノート**である。
  - オートメーションは、直前の値とオートメーションノート自身の値(直後値)を参照し、その間を滑らかに(実際には`#AutoResol`で定義された間隔で)変化させる。
  - 数式参照を伴う値は**その位置で変化が完了する**ことを意味する。
    - 例: `100, $1:200`
      - `,`が1個あるので、この小節は2等分される(1拍目と3拍目で値が適用される)。
      - 1拍目(小節頭)において`100`が適用される。
      - その後3拍目にかけて滑らかに`200`へと近づいていき、3拍目(=1小節の**ちょうど真ん中**)で`200`になる。
    - まるごと1小節かけて変化させる場合、`#001 Bpm 100` `#002 Bpm $1:200`のように2行に分けて書く必要がある。
    - 実際に適用される値は、直前値を`a`、直後値を`b`として`a + (b - a) * expr(t)`となる。
  - 数式の評価後、値は`0`以上`1`以下となるように矯正される。
  - 具体例:
    ```
    #AutoResol 1/32
    #DefExpr 1 t
    #001 Bpm 100, $1:200
    ```
    - `#AutoResol 1/32`: オートメーションは32分音符間隔で適用される。
    - `#DefExpr 1`の内容`t`は`t`の値をそのまま返す、すなわち直線的な変化を表す。
    - オートメーション区間は`1/2`(四分音符2個ぶん)
      - 変化回数は`(1/2) / (1/32) = 16`回。
      - `t`には`1/16`から`15/16`までが15段階で代入される。
    1. `#001`の最初でBPMが`100`になる。
    2. 32分音符(`0.075`秒)経過後、テンポは`100 + (200 - 100) * (1 / 16) = 106.25`になる。
    3. 32分音符2個(`0.0706`秒/累計`0.1456`秒)経過後、テンポは`100 + (200 - 100) * (2 / 16) = 112.5`になる。
    4. 以降32分音符間隔で変化。
    5. 最初から2分音符(`0.8508`秒)経過後、3拍目の時点でテンポは`200`になる。
  - 数式の内容に依らず、オートメーションノートが存在するタイミングにおいてはそのノート自身の値が適用される。また、オートメーション式は`t = 0`においては適用されない。
    - 例: `#001 Bpm 100, $1:200`
    - この記述は「1拍目から3拍目にかけて`100`から`200`に変化させる」という意味である。
    - `#DefExpr 1 0`の場合:
      - `a + (b - a) * 0 = a`であるため、1拍目と3拍目の間は常に`100`が適用される。
      - 3拍目において数式とは無関係に`200`が適用される。
      - 従ってこの場合は`#001 Bpm 100, 200`と同じ結果になる。
    - `#DefExpr 1 1`の場合
      - `a + (b - a) * 1 = b`であるため、数式を適用すると常に`200`となるが、数式は**直前値から`#AutoResol`が経過した時**に初めて適用される。
      - この場合、1拍目に`100`が適用され、その32分音符(`0.075`秒)経過後に`200`となり、それ以降`200`のままになる。
  - 以下のいずれかの場合、オートメーションは行われず、指定された数値が直接適用される。
    - 「直前値」が存在しない場合
    - 参照先の`#DefExpr`が未定義の場合
    - `#000 Bpm $1:120`は`#000 Bpm 120`と同じ意味である。
    - `#002 Bpm $2:180`は`#DefExpr 2`が未定義の場合は`#002 Bpm 180`と同じ意味になる。
  - オートメーションノート自身が値を伴わないのは不正な表現であり、その部分を`0`(存在しない)とみなす。
    - `100, $1, 200, 0`の`$1`の部分は変化後の値が欠けているため無視される。
    - 「1拍目から3拍目にかけて`100`から`200`に変化する」を表したい場合、数式参照は`200`の直前に書く: `100, 0, $1:200, 0`
  - 同一チャンネル・同一タイミングに複数のオートメーションノートが存在する場合、原則に従って**記述された順に全てを適用**する。
    - 例:
    ```
    #001 Bpm 100, 0, $1:200, 0
    #001 Bpm 0, 0, $2:150, 0
    ```
    - 1拍目: 1行目が「`100`にする」、2行目が「変化させない」という内容なので、最終的には`100`が採用される。
    - 3拍目: 1行目が「`#DefExpr 1`に従って`200`まで変化させる」、2行目が「`#DefExpr 2`に従って`150`まで変化させる」という内容である。
    - `$1:200`に対する**直前値**は1拍目に存在する`100`である。従って「1拍目から3拍目にかけて`100`から`200`に変化する」というオートメーションが適用される。
    - `$2:150`に対する**直前値**は3拍目に存在する`$1:200`である。従って「3拍目から3拍目にかけて`200`から`150`に変化する」というオートメーションが適用される。ただしその期間が`0`であるため、実際には`150`が直接適用される。
    - 最終的には「1拍目から3拍目にかけて`100`から`200`に変化」「3拍目では`200`ではなく`150`になる」という挙動になる。

## 分岐コマンド
- 特定の条件で変化する譜面などを表現するためのコマンド。
- 分岐コマンドは最低1組の**開始コマンド**と**終了コマンド**からなり、その間に置かれたシーケンスコマンドを一纏めに(スコープ化)する。
- 分岐コマンドの記法は**メタコマンド**と同様であり、分岐コマンドとメタコマンドはその識別子によってのみ区別できる。
- 分岐コマンドは入れ子にできる。分岐が入れ子になっている場合、最も内側のスコープは全ての条件が満たされた場合にのみ適用される。

### 規定の分岐コマンド
解釈器は少なくとも以下の分岐コマンドを解釈できなければならない[_**MUST**_]。
- `#IF`
  - 例: `#IF $1 == 1`
  - コマンドの内容は**数式**(後述)である。
  - **数式の評価(計算)結果が`0`以外の場合**、対応する`#EndIF`/`#Else`/`#ElseIF`までの区間を適用する。
    - `#IF 1`は**常に適用される分岐**であり、`#IF 0`は**決して適用されない分岐**となる。
  - `#IF`系のコマンドは**必ず`#EndIF`で閉じなければならない**。
    - もし複数の`#IF`が連続で出現した場合、それらは独立の条件ではなく**ネストした条件分岐**とみなされる。
    - 分岐区間がファイルの終端までの場合に限り、`#EndIF`は省略可能である(が、一貫性のために省略するべきではない)。
  - `#IF`の数式は、そこで参照される乱数値も含めて**楽曲開始前**に評価される。
    - `COMBO`のような特殊変数は全て初期値(基本的には`0`)として計算される。
- `#ElseIF`
  - 例: `#ElseIF $1 == 2`
  - コマンドの内容は**数式**(後述)である。
  - 対応する`#IF`が**適用されなかった場合**に数式が評価され、**結果が`0`以外の場合**、対応する`#Else`/`#EndIF`までの区間を適用する。
    - `#ElseIf`に書かれた数式は、対応する`#IF`が偽(`0`)だった場合にのみ評価される。
    - `#IF`の枝が採用された場合、対応する`#ElseIF`の数式は**一切評価されない**。
  - 対応する`#IF`が見つからなかった場合、解釈器は以下のいずれかで対応する。
    - これを最初の`#IF`とみなす。[_**SHOULD**_]
    - そのBMSTを不正なものとみなす。
- `#Else`
  - 対応する`#IF`及び`#ElseIF`の**いずれも適用されなかった場合**、対応する`#EndIF`までの区間を適用する。
- `#EndIF`
  - 対応する`#IF`/`#ElseIF`/`#Else`の分岐区間の終了を示す。
  - `#IF`節は`#IF`で始まり、0個以上の`#ElseIF`と0個か1個の`#Else`を挟み、`#EndIF`で終わる。以下は正当な`#IF`節の例である。
```
#IF $1 == 1
  (内容)
#ElseIF $1 == 2
  (内容)
#ElseIF $2 > $1
  (内容)
#Else
  (内容)
#EndIF
```
- `#LazyIF`
  - 例: `#LazyIF COMBO >= 100 @16`
  - 基本的には`#IF`と同じだが、式の評価を**指定された判定タイミングまで遅らせる**。
  - コマンドの内容は`(数式)@(判定タイミング)`という形式である。
    - `@`以降が存在しない場合や`0`以上の数値として解釈できない場合、`@0`として扱う。
    - `@`が複数出現する場合、最初の部分だけがタイミングとして評価される。
  - 楽曲再生中、**指定のタイミングに達した時点**で数式の値を評価し、**結果が`0`以外の場合**、対応する`#Else`/`#EndIF`までの区間を適用する。
    - `@16`と書かれている場合、**16小節目の最初**に、**それ以外の全て(演奏ノートの処理、BGAの更新、テンポの変更など)より前**に評価する。
    - 指定タイミングが`0`以下である場合は`#IF`と同じタイミングで評価する。
    - タイミングが整数値でない場合、小数部分は小節内位置(相対)を表すものとみなす。
      - `@16.5`は通常「16小節の2拍経過時」を表すが、小節長が変更されている場合(例:`#016 Bar 3/4`)はそれに応じて変化する(例:16小節の1.5拍経過時)。
    - タイミングは小数表現(`16.5`)、分数表現(`33/2`)の他に小節位置(`#016:1/2`)でも表記できる。[_**MUST**_]
      - 上記の例では3つとも同じタイミングを表している。
    - 分岐内に`#LazyIF`で指定されたタイミングより以前のシーケンスコマンドが存在する場合、**それらは全て無いものと扱う**。
  - `#LazyIF`は`#IF`と同様に`#EndIF`で閉じる。`#ElseIF`と`#Else`で別の分岐を付加することもできる。
    - `#LazyIF`に付随する`#ElseIF`は同じタイミングで数式を評価する。
    - `#LazyIF`の枝が採用された場合、対応する`#ElseIF`の数式は**一切評価されない**。
  - 数式中に登場する`#DefVar`(`$001`などで参照)の値は**実際に必要になった時に初めて評価する**。
    - 例: `#DefVar 003 COMBO`
    - `COMBO`は評価時点でのコンボ数を取得する特殊変数(後述)。
    - この変数が`#IF`から参照される場合、タイミングが`0`(=楽曲開始)の時点ではコンボ数は`0`であるため、`$003 = 0`となる。
    - `#LazyIF`から参照される場合、例えばタイミングが`@16`であれば、`$003`の値は16小節目開始時点でのコンボ数となる。
    - 一度評価された変数は**その後何度参照されても再評価しない**。すなわち以下のような記述が可能である。
```
#DefVar 001 COMBO
#DefVar 002 COMBO

// $1 = 16小節開始時点でのコンボ数
#LazyIF $1 > 0 @16
#EndIF
// $2 = 64小節開始時点でのコンボ数
// 「$2 - $1」は「64小節開始時点でのコンボ数 - 16小節開始時点でのコンボ数」
// すなわち「16小節開始から64小節開始までに増加したコンボ数」という意味になる。
#LazyIF $2 - $1 >= 100 @64
  (内容)
#EndIF
```

# 付録
## 再生可能な解釈器に要求する基本仕様
`BMST`を演奏するプログラムは、原則として以下の規定に従う。[_**SHOULD**_]
- 判定
  - ノートを処理する際の判定として、少なくとも以下の4種類を実装する。
    - 成功(P.GREATなど)
      - ノートが正しいタイミングで処理されたことを示す。
      - ノート1個につき`#Recover`で指定された量だけゲージが増加する。
        - 解釈器は、この値を適当に(決定論的に)加工して使用してもよい。
        - 解釈器は、この値をそのまま採用するプレイモードを最低でも1つ用意する。
      - 成功判定はタイミングのズレに応じて複数段階あってもよい。
        - この場合、`#Recover`の回復量は**最も厳しい成功判定**(最高判定)でのゲージ回復量として採用する。
      - 真のタイミングに対して、成功判定は少なくとも **前後に`7ms`** (約`1/60`秒)の幅を持たなければならない。
    - 失敗(BAD)
      - ノートは処理されたが、そのタイミングがズレ過ぎていることを示す。
      - 1個のノートに対して、ボタンを押したタイミングが成功判定期間と失敗判定期間の両方に含まれる場合、成功判定を優先する。
    - 見逃し(POOR)
      - ノートが処理されず、成功と失敗いずれの判定期間も過ぎた場合に発生する。
      - ノートが判定ラインを通過する前には発生しない。
      - 1個のノートに対して1度だけ発生する。
      - 成功/失敗/見逃し判定は、1個のノートに対していずれか1つのみが発生し、同種の判定が重複して適用されることもない。
    - 空打ち(空POOR)
      - 成功と失敗いずれの判定期間でもないタイミングでボタンを押した際に発生する。
      - 1個のノートに対して複数回発生してもよい。
      - 空打ち判定はノートを処理しない。成功/失敗判定とは個別に発生する。
        - 判定ライン通過前に空打ち(早POOR)が発生したノートは、判定ラインの付近で改めて成功/失敗/見逃しの判定を行う。
        - 成功または失敗として処理されたノートの直後に同じボタンを再び押した場合、空打ち判定が発生する(遅POOR)。
        - 見逃しとして処理されたノートの直後では空打ち判定は発生しない。
  - 以上4種の判定を実装しなければならないが、成功判定を除き、それぞれの幅はゲームモードに応じて`0`を含む任意の期間にしてよい。
    - 失敗判定の幅は成功判定の幅より狭くてもよい。この場合、失敗判定は発生しなくなる。
    - 空打ち判定の幅は無限大としてもよい。この場合、未処理のノートが残っているか、最後に処理したノートが成功/失敗判定である限り、ボタンを押した際には毎回空打ちが発生する。
  - 判定は1回のボタン押下につき最大でいずれか1個だけが発生する。
    - 例: 複数のノートが空打ち範囲内にある状態で対応するボタンを押した場合でも、発生する空打ち判定は1回である。
    - 例: 1個目のノートを成功で処理した直後にボタンを押した際、2個目のノートの失敗範囲内であれば、2個目の失敗判定が優先され、1個目に対する空打ち判定は発生しない。
    - 例: 2個のノートの成功範囲が重なるタイミングでボタンを押した場合、より判定ラインに近いほうのノートの判定のみが発生し、2個目のノートは改めて処理する(または見逃す)必要がある。
      - 2個のノートのタイミングが完全に等しい場合、解釈器はそれらを1個のノートとして扱ってもよい。[_**MAY**_]
      - この場合、ノート自体は1個として扱われるが、ボタンを押した際に再生される音声は重なっているノートを全て合わせたものになる。
- ノート
  - 実際のゲームでプレイヤーが操作するべき対象。
    - 基本的には「特定のタイミングで対応するボタンを操作することで、ノートごとに設定された音声を再生する」という処理を想定するが、「ボタンの操作」及び「音声の再生」はそれぞれ解釈器によって異なる処理でもよい(例: 「画面をタッチすることでノート周辺が光る」など)。
  - プレイエリアに流れてくるノートとして、少なくとも以下の4種類を実装する。
    - 通常ノート
      - ノートが判定ラインを通過するタイミングで対応するボタンを押す。
      - 総ノート数やコンボ数に影響する。
      - 通常ノートの前後、成功/失敗/空打ちの期間内に対応するボタンを操作した場合、そのノートに設定された音声を再生する(あるいは特定のリアクションを起こす)。
    - 不可視ノート
      - ノートは表示されず、判定処理も行わない。
      - 総ノート数やコンボ数には影響しない。
      - 不可視ノートを表示してもよい。その場合でも判定処理は行わない。
        - 不可視ノートを表示する場合、通常ノートを隠してはならない。
      - 不可視ノートの前後、成功/失敗/空打ちの期間内に対応するボタンを操作した場合、そのノートに設定された音声を再生する。ただし、通常ノートの判定期間と重なる場合、通常ノートを優先する。
    - ロングノート
      - 始点と終点を持つ。
      - 始点が判定ラインを通過するタイミングで対応するボタンを押し、押したままにする。
      - 終点が判定ラインを通過するタイミングで、押しているボタンを離す。
      - 以下の要素はロングノートモード(`#LNMODE`)に応じて決定する。
        - 始点と終点をそれぞれ1個のノートとして数えるか、ロングノート全体で1個のノートと見なすか
        - 途中でボタンを押した場合や、始点で押さず途中から押した場合の処理
        - ボタンを押している間のゲージやコンボ数の推移
    - 地雷ノート
      - ノートが判定ラインを通過するタイミングで対応するボタンを離しておく。
      - ボタンを押していた場合、各ノートに設定された値に応じてペナルティを与える。
      - ペナルティの具体的な内容は解釈器が独自に設定してよい。
      - ペナルティが実際に発生する場合は、地雷ノートは他の種類のノートと区別可能な状態で見えなければならない。
  - ゲームの機能を持たない解釈器(エディターなど)は、少なくとも上記のノートを全て区別可能な方法で表現する(見た目を変えるなど)。
- ロングノートモード
  - いずれの場合も、終点のノートを処理する際にそのノートの音声を再生するかどうかは解釈器が自由に決定してよい。
    - 譜面作者は、ロングノート終端で音が鳴ることを避けたい場合、音声が定義されていない番号を使用するべきである。
  - ロングノートモードとして少なくとも以下の2種類を実装する。
    - 1個のノート (LN)
      - 始点に対する判定を仮判定とする。
        - 仮判定が失敗または見逃しの場合、そこで判定を確定し、終点では判定を行わない。
      - 終点自体はコンボ/総ノート数に計上しない。
        - 終点の成功判定期間より前にボタンを離した場合、仮判定を見逃しに変更し、確定する。
        - 終点の成功判定期間内にボタンを離した場合、またはボタンを押した状態で終点が判定ラインを通過した場合、仮判定をそのままロングノート全体の判定として確定する。
    - 2個のノート (CN)
      - 始点は通常ノートと同じように判定する(1コンボに数える)。
        - 始点を処理した時点で始点の判定を確定し、コンボやスコアに計上する。
        - 始点が失敗または見逃し判定の場合、終点の判定は行わない(ミス扱いにもしない)。
      - 終点について個別の判定を行う(1コンボに数える)。
        - 終点の成功判定期間より前にボタンを離した場合、終点は見逃し判定になる。
        - 一度ボタンを離した場合、再び押しても終点の判定は変わらない。
        - 終点の成功判定期間内にボタンを離した場合、終点は成功判定になる。
        - ボタンを押した状態で終点が判定ラインを通過し、そのまま成功判定期間を過ぎた場合、終点は失敗判定になる。
        - ニ方向入力が可能なデバイスの場合、「離した」は「逆方向への入力開始」も含む。
    - 2個のノート+持続判定 (HCN)
      - 基本的にはCN(2個のノート)と同じ。
      - ロングノートの中間(始点と終点を除く部分)では一定間隔(例:16分音符間隔)でボタンの押下状態をチェックする。
        - ボタンが押されている場合は仮成功判定とし、ゲージを増加する。
        - ボタンが押されていない場合は仮失敗判定とし、ゲージを減少する。
      - 仮成功/仮失敗は通常の判定のいずれかと同一でもよいし、それらとは別の判定として用意してもよい。
  - ロングノート終点の成功判定期間は、通常の成功判定期間とは異なってもよい。
- BGAエリア
  - プレイ中に流れる楽曲独自の映像(BGA)を表示する領域を、0個以上用意する。
    - BGAエリアの縦横比は解釈器が自由に決定する。
    - BGAは表示しなくてもよい。BGAエリア自体の透過率は任意に設定できる。
  - BGAエリアとそこに表示する映像の縦横比が異なる場合、以下のいずれかの方法で表示する。
    - 例: BGAエリアが`100x100`、表示しようとしている画像が`200x50`の場合
    - 最小スケール
      - 縦と横のうち、拡大率が小さくなるほうに合わせて全体を拡縮し、エリアの中央に表示する。
      - 例の場合、画像を`100x25`に縮小した上で、BGAエリアの`0,37.5`を起点として描画する。
    - 最大スケール
      - 縦と横のうち、拡大率が大きくなるほうに合わせて全体を拡縮し、エリアの中央に表示する。
      - 拡縮の結果BGAエリアをはみ出る場合、はみ出た部分の処理は解釈器が独自に決定してよい。
      - 例の場合、画像を`400x100`に拡大した上で、BGAエリアの`-150,0`を起点として描画する。
    - 個別拡縮
      - 縦と横をそれぞれBGAエリアと同じになるように拡縮して表示する。
      - 例の場合、画像を`100x100`に変形(横は半分に縮小、縦は2倍に拡大)した上で、BGAエリアの`0,0`を起点として描画する。
      - この場合元々の縦横比は保存されない。
    - いずれの場合も、拡縮の方法や拡縮後のサイズや座標が非整数となった場合の処理などは全て解釈器が独自に決定してよい。
  - 少なくとも2つ以上の通常レイヤーと1つ以上の**ミスレイヤー**を実装する。
    - 異なるレイヤーには同時に異なる画像を描画できる。
    - 表示する画像がアルファチャンネルを含む場合には、それに応じて透過する。
    - ミスレイヤーは全ての通常レイヤーより上に描画される。
    - ミスレイヤーは、典型的にはミス(ノートを失敗または見逃し)した際に一定期間表示されるが、具体的な運用は解釈器が独自に決定してよい。
  - 各レイヤーにおいて、画像は以下の規則で描画される。
    - そのレイヤーに対する描画コマンドが無い限り、そのレイヤーは完全な透明である。
    - そのレイヤーに対する描画コマンドが処理された場合、次に描画コマンドが処理されるまでは**その画像を表示し続ける**。
    - 動画の場合、基本的には描画コマンドの時刻を起点として動画の最初から再生し、最後まで再生した後は**最後のフレームを表示し続ける**(自動的に消さない)。
    - 空の定義の場合や対象のファイルが存在しない場合、その描画コマンドが処理される際にはそれまで表示していた画像を消去する。

## 数式